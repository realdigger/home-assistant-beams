## 0.8.3

- PPFD-сенсоры теперь отображают значения целыми числами без дробной части.
- Округление применяется только к отображаемому значению сенсоров; внутренний расчёт PPFD не изменён.

## 0.8.2

- Улучшено получение PPFD @35 см и PPFD @45 см: интеграция теперь ищет высотные коэффициенты не только на уровне канала, но и во вложенных LED-записях `/api/kit`.
- Удалены приблизительные расчёты по обратному квадрату расстояния; используются только реальные данные контроллера.

# Changelog RU

## 0.8.1

- Исправлен fallback для `PPFD @35 см` и `PPFD @45 см`: удалён расчёт по обратному квадрату расстояния, так как для протяжённого аквариумного светильника он занижает значения.
- Для `@35 см` и `@45 см` теперь используются только точные коэффициенты или коэффициенты отношения, если контроллер отдаёт их в API.
- Если точных данных для `@35 см`/`@45 см` нет, сенсор остаётся `unknown`, а не показывает неверную оценку.
- В атрибуте `source` у PPFD-сенсоров теперь видно, почему значение не рассчитано.

## 0.8.0

- Исправлено отображение PPFD: для `@25 см` добавлен расчёт по `kit.channels[*].totalPPFD`, как в расчёте DLI.
- Для `@35 см` и `@45 см` добавлен fallback-расчёт от `@25 см`, если прошивка не отдаёт отдельные коэффициенты PPFD для этих высот.
- Для сенсоров PPFD добавлен атрибут `source`, показывающий источник значения.

## 0.7.9

- Убрана единица измерения у `sensor.beams_light_current_cycle_dli`: теперь отображается только число без `mol/m²/day`.

## 0.7.8

- В `README.md` добавлена плашка актуального GitHub release.
- Ссылки на репозиторий обновлены на `https://github.com/realdigger/home-assistant-beams`.

## 0.7.7

- DLI текущего цикла теперь округляется до десятых.

## 0.7.6

- Изменён формат отображения `sensor.beams_light_uptime`: теперь выводится строка в формате `дни часы минуты`, например `2 д 5 ч 17 мин`.
- Изменён формат отображения `sensor.beams_light_cycle_timepoint`: теперь выводится строка в формате `часы минуты`, например `12 ч 30 мин`.

## 0.7.5

- В `README.md` добавлен disclaimer: проект не связан с BeautifulReef и BEAMS, не одобрен ими и не поддерживается ими.

## 0.7.4

- Из `README.md` удалена фраза про локальную работу через HTTP API и отсутствие облачного доступа.

## 0.7.3

- Сокращён `README.md`: удалены второстепенные сведения, длинный список внутренних API endpoint-ов, подробные ограничения и избыточные пояснения.
- Оставлены только основные разделы для GitHub/HACS: описание, установка, настройка, сущности, сервисы, troubleshooting и лицензия.

## 0.7.2

- Исправлено получение DLI текущего дневного цикла: значение теперь рассчитывается так же, как в штатном web-интерфейсе, из точек `dailyCycle.spectrums`, `kit.channels[*].totalPPFD`, коэффициента `math.led_correction.value` и поправки мастер-настройки аквариума, если она заполнена.
- Для сенсора `sensor.beams_light_current_cycle_dli` добавлен атрибут `source`, показывающий источник значения DLI.
- Старый поиск готового поля `dli` оставлен только как fallback для прошивок, которые отдают предрассчитанный DLI в API.

## 0.7.1

- Добавлен диагностический сенсор модели светильника из поля `kit_name` ответа `GET /api/kit`: `sensor.beams_light_light_model`.
- Добавлен диагностический сенсор времени работы контроллера: `sensor.beams_light_uptime`.
- Для `uptime` добавлен универсальный поиск по типовым полям API: `uptime`, `upTime`, `uptimeSeconds`, `uptimeMs`, `systemUptime`, `runtime`, `runTime`, `aliveTime`, а также по boot timestamp в `/info`, если он есть.
- `/info` теперь дополнительно читается при регулярном обновлении, чтобы `uptime` мог изменяться без перезапуска Home Assistant.

## 0.7.0

- Пакет переименован в `BEAMS Light`.
- Домен интеграции изменён на `beams_light`.
- Удалены упоминания прежнего названия контроллера из кода, переводов, README, changelog и HACS metadata.
- Добавлена лицензия MIT: `LICENSE`.

## 0.6.1

- Добавлен `hacs.json` в корень пакета/репозитория для установки через HACS как custom repository.
- В `manifest.json` обновлена версия до `0.6.1`.
- В `manifest.json` добавлены GitHub-ссылки на документацию и issue tracker, а также code owner `@realdigger`.
- В пакет включён обновлённый `README.md` для GitHub/HACS.

## 0.6.0

- Добавлены отдельные сенсоры текущего PPFD для высот `@25cm`, `@35cm` и `@45cm`:
  - `sensor.beams_light_ppfd_25cm`;
  - `sensor.beams_light_ppfd_35cm`;
  - `sensor.beams_light_ppfd_45cm`.
- PPFD рассчитывается по текущим каналам светильника и коэффициентам BEAMS из `/api/math/get`, если они доступны.
- Для `@25cm` добавлен fallback по `totalPPFD` из совпавшего спектра галереи, если коэффициенты PPFD недоступны.
- Добавлены русские и английские переводы названий новых сенсоров.

## 0.5.0

- Добавлено отдельное отображение DLI текущего дневного цикла: `sensor.beams_light_current_cycle_dli`.
- Исправлено получение DLI: теперь значение ищется не только в `full.dailyCycle.dli`, но и в нескольких вариантах структуры ответа BEAMS.
- Сенсор `DLI` теперь явно называется «DLI текущего цикла».

## 0.4.0

- Добавлена поддержка галереи спектров через `GET /api/spectrums`.
- Добавлен выбор спектра через сущность `select.*_spectrum`.
- Добавлен сервис `beams_light.apply_spectrum` для применения спектра по имени, подписи варианта или числовому ID.
- Добавлен сервис `beams_light.refresh_spectrums` для перезагрузки списка спектров без перезапуска Home Assistant.
- При применении спектра интеграция отправляет массив каналов в `POST /api/channels/set` и переводит светильник в ручной режим, чтобы выбранный спектр сразу применился.
- В атрибуты основной light-сущности добавлена информация о текущем совпавшем спектре: название, ID и `total_ppfd`.

## 0.3.0

- Исправлен порядок переключения в ручной режим по поведению штатного web-интерфейса: сначала `POST /api/channels/set`, затем `POST /api/state/set {"manual": true}`.
- Интервал обновления установлен 3 секунды.

## 0.2.0

- Значения каналов берутся из `GET /api/channels/get`.
- Добавлены диагностические сенсоры DLI, расчётной мощности и точки дневного цикла.

## 0.1.0

- Первая версия custom integration для BEAMS.
